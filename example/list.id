func (range start end step) {
    let i = start;
    let list = [];
    while (start <= i < end) {
        let list = list + i;
        let i = i + step
    };
    (list)
};

print { cast { range 1 10 1 } to symbol }
