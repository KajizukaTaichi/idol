func (range start end step) {
    let i = start;
    let list = [];
    while (start <= i < end) {
        let list = list + i;
        let i = i + step
    };
    (list)
};

func ( dbg value ) {
    print "Debug: ";
    print { cast value to symbol };
    print new-line
}
